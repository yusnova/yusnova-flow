import { BookingFlow } from "@/components/BookingFlow"

export default function HomePage() {
  return (
    <main className="min-h-screen bg-gradient-to-b from-slate-950 to-surface">
      <BookingFlow />
    </main>
  )
}
